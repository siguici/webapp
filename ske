#!/usr/bin/env php
<?php

$organizer = require_once __DIR__ . DIRECTORY_SEPARATOR . 'bootstrap.php';

$cmd = $argc > 1 || strpos($argv[1], '-') === 0 ? 'app' : $argv[1];

require (fn() => __DIR__ . "/bundle/src/main/cli/$cmd.php")();
