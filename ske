#!/usr/bin/env php
<?php

$organizer = require_once __DIR__ . DIRECTORY_SEPARATOR . 'bootstrap.php';

require (fn() => __DIR__ . '/bundle/src/main/cli/' . ($argc > 1 ? $argv[1] : $argv[0]) . '.php')();
